<UserControl x:Class="Type4Me.Views.Settings.VocabularyTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#FAF5ED"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style x:Key="SaveBtn" TargetType="Button">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AddBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#4D9E59"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,0,8,0">

            <!-- Hotwords -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitle}" x:Name="HotwordsTitle"/>
                    <TextBlock x:Name="HotwordsDesc" FontSize="11" Foreground="#6B6B6B" Margin="0,0,0,8"
                               TextWrapping="Wrap"/>
                    <TextBox x:Name="HotwordTextBox" AcceptsReturn="True" MinHeight="100"
                             VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"
                             FontSize="12"/>
                    <Button Style="{StaticResource SaveBtn}" x:Name="SaveHotwordsBtn"
                            Margin="0,8,0,0" HorizontalAlignment="Left"
                            Click="SaveHotwords_Click"/>
                </StackPanel>
            </Border>

            <!-- Snippets -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <DockPanel Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource SectionTitle}" DockPanel.Dock="Left"
                                   x:Name="SnippetsTitle"/>
                        <Button Style="{StaticResource AddBtn}" DockPanel.Dock="Right"
                                HorizontalAlignment="Right" x:Name="AddSnippetBtn"
                                Click="AddSnippet_Click"/>
                    </DockPanel>
                    <TextBlock x:Name="SnippetsDesc" FontSize="11" Foreground="#6B6B6B"
                               Margin="0,0,0,8" TextWrapping="Wrap"/>

                    <!-- Snippet header -->
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="30"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" x:Name="TriggerHeader" FontSize="11"
                                   FontWeight="SemiBold" Foreground="#3D3D3D"/>
                        <TextBlock Grid.Column="1" x:Name="ValueHeader" FontSize="11"
                                   FontWeight="SemiBold" Foreground="#3D3D3D" Margin="8,0,0,0"/>
                    </Grid>

                    <ItemsControl x:Name="SnippetsList"/>
                </StackPanel>
            </Border>

            <!-- Boosting Table ID -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitle}" x:Name="BoostingTitle"/>
                    <TextBlock x:Name="BoostingDesc" FontSize="11" Foreground="#6B6B6B"
                               Margin="0,0,0,8" TextWrapping="Wrap"/>
                    <TextBox x:Name="BoostingIdBox" FontSize="12" MaxWidth="400"
                             HorizontalAlignment="Left"/>
                    <Button Style="{StaticResource SaveBtn}" x:Name="SaveBoostingBtn"
                            Margin="0,8,0,0" HorizontalAlignment="Left"
                            Click="SaveBoosting_Click"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
